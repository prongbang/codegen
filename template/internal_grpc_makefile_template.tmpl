gen:
	protoc --go_out=. --go_opt=paths=source_relative \
		--go-grpc_out=. --go-grpc_opt=paths=source_relative,require_unimplemented_servers=false \
		./$(service)/$(version)/$(service).proto

gen_health:
	make gen service=health version=v1
