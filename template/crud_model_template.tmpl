package {{.PackageName}}

import (
	{{- range .Imports }}
	"{{ . }}"
	{{- end }}
	"{{ .Module }}/pkg/core"
)

{{- $fields := .Fields }}
type {{.ModelName}} struct {
{{- range $field := $fields }}
	{{- if $field.PrimaryKey }}
    {{ $field.Name }} {{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}"`
    {{- else }}
	{{- if contains $field.Type "*" }}
    {{ $field.Name }} {{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}"`
    {{- else }}
    {{ $field.Name }} *{{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}"`
    {{- end }}
	{{- end }}
{{- end }}
}

type Data{{.ModelName}} struct {
	{{.ModelName}}
}

type Paged{{.ModelName}} core.Paging

type Lite{{.ModelName}} struct {
{{- range $field := $fields }}
	{{ $field.Name }} {{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}"`
{{- end }}
}

type Create{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
{{- range $field := $fields }}
	{{- if $field.PrimaryKey }}
    {{ $field.Name }} {{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}"`
    {{- else }}
	{{ $field.Name }} {{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}" validate:"required"`
	{{- end }}
{{- end }}
}

func (m Create{{.ModelName}}Request) To{{.ModelName}}() {{.ModelName}} {
    return {{.ModelName}}{
    {{- range $field := $fields }}
        {{- if $field.PrimaryKey }}
        {{ $field.Name }}: m.{{ $field.Name }},
        {{- else }}
        {{- if contains $field.Type "*" }}
        {{ $field.Name }}: &m.{{ $field.Name }},
        {{- else }}
        {{ $field.Name }}: &m.{{ $field.Name }},
        {{- end }}
        {{- end }}
    {{- end }}
    }
}

type CreateList{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
	{{.ModelName}}s []Create{{.ModelName}}Request
}

type Update{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
{{- range $field := $fields }}
	{{- if $field.PrimaryKey }}
    {{ $field.Name }} {{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}" validate:"required"`
    {{- else }}
	{{ $field.Name }} *{{ $field.Type }} `json:"{{ $field.JsonTag }}" db:"{{ $field.DbTag }}"`
	{{- end }}
{{- end }}
}

func (m Update{{.ModelName}}Request) To{{.ModelName}}() {{.ModelName}} {
    return {{.ModelName}}{
    {{- range $field := $fields }}
        {{- if $field.PrimaryKey }}
        {{ $field.Name }}: m.{{ $field.Name }},
        {{- else }}
        {{- if contains $field.Type "*" }}
        {{ $field.Name }}: m.{{ $field.Name }},
        {{- else }}
        {{ $field.Name }}: m.{{ $field.Name }},
        {{- end }}
        {{- end }}
    {{- end }}
    }
}

type UpdateList{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
	{{.ModelName}}s []Update{{.ModelName}}Request
}

type Delete{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
	{{- if .PrimaryField.Name }}
	{{ .PrimaryField.Name }} {{ .PrimaryField.Type }} `json:"{{ .PrimaryField.JsonTag }}" validate:"required"`
	{{- end }}
}

type List{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
	List{{.ModelName}}Criteria
}

type LiteList{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
	LiteList{{.ModelName}}Criteria
}

type One{{.ModelName}}Request struct {
	UserRequestInfo *core.UserRequestInfo `json:"-"`
	One{{.ModelName}}Criteria
}

type List{{.ModelName}}Criteria struct {
	core.Params
}

type LiteList{{.ModelName}}Criteria struct {
	core.Sorting
}

type One{{.ModelName}}Criteria struct {
    {{- if .PrimaryField.Name }}
    {{ .PrimaryField.Name }} {{ .PrimaryField.Type }} `json:"{{.PrimaryField.JsonTag}}" validate:"required"`
    {{- end }}
}
